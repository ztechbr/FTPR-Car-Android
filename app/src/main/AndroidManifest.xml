<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- RZ - Permissões de rede e localização necessárias para o funcionamento do app -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApiTest"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- RZ - Cole sua API KEY do Google Maps aqui embaixo -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyA2gDTVvOt6jIbP8emoHgLza-CXP-2y8Fs" />

        <!-- RZ - Activity de Login definida como Launcher do aplicativo -->
        <activity
            android:name=".ui.login.LoginActivity"
            android:theme="@style/Theme.MyApiTest"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- RZ - Activity Principal para exibir a lista de carros -->
        <activity
            android:name=".ui.main.MainActivity"
            android:theme="@style/Theme.MyApiTest"
            android:exported="false"
            android:label="@string/app_name" />

        <!-- RZ - Activity para cadastrar um novo carro -->
        <activity
            android:name=".ui.main.AddCarActivity"
            android:theme="@style/Theme.MyApiTest"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- RZ - Activity para selecionar localização manualmente no mapa -->
        <activity
            android:name=".ui.main.SelectLocationActivity"
            android:theme="@style/Theme.MyApiTest"
            android:exported="false" />

        <!-- RZ - Activity para exibição da localização no Google Maps -->
        <activity
            android:name=".ui.main.CarMapActivity"
            android:theme="@style/Theme.MyApiTest"
            android:exported="false" />

    </application>

</manifest>